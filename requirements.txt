# Audio-memory-enhancer — Python dependencies.
#
# IMPORTANT: This project REUSES the existing venv at:
#   C:\Users\Yifan\venvs\audio_transcribe\
# which already has every package below proven working (shared with
# audio_transcribe_notes). DO NOT create a new venv or re-install these —
# version mismatches will break the shared ASR pipeline.
#
# This file is a REFERENCE of what the pipeline needs, not an install manifest.
# If you ever need to rebuild the venv from scratch, the install commands are:
#
#   python -m venv C:\Users\Yifan\venvs\audio_transcribe
#   C:\Users\Yifan\venvs\audio_transcribe\Scripts\activate.bat
#   pip install torch torchaudio --index-url https://download.pytorch.org/whl/cu126
#   pip install qwen-asr silero-vad pyannote-audio
#   pip install Pillow pillow-heif openai pyyaml jinja2 librosa numpy
#
# Verified versions in the shared venv (as of project setup):
torch==2.11.0+cu126
torchaudio==2.11.0+cu126
qwen-asr==0.0.6
openai==2.30.0
pyannote-audio==4.0.4
silero-vad==6.2.1
librosa==0.11.0
Pillow==12.1.1
pillow-heif==1.3.0
PyYAML==6.0.3
Jinja2==3.1.6
numpy==2.4.3
